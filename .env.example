# STOCK-BOT Environment Example (DO NOT USE IN PRODUCTION AS-IS)
#
# Copy to `/root/stock-bot/.env` on the droplet and fill real values.
# This file is intentionally safe to commit (no secrets).
#
# NOTE: The dashboard enforces HTTP Basic Auth and FAILS CLOSED if
# DASHBOARD_USER / DASHBOARD_PASS are missing or empty.

# --- Dashboard (HTTP Basic Auth) ---
DASHBOARD_USER=you@example.com
DASHBOARD_PASS=change_me_to_a_strong_password

# --- Trading (examples only) ---
ALPACA_KEY=
ALPACA_SECRET=
ALPACA_BASE_URL=https://paper-api.alpaca.markets
UW_API_KEY=

