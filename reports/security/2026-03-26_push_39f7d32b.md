## Security Review (push to main)

**Commit / ref:** `39f7d32be2032eae8d3b314691610826376853ad`  
**Pushed by:** mlevitan96-crypto  
**Date:** 2026-03-26  
**Commit message:** Alpaca forward truth contract: runner, systemd timer, bounded repair, deploy script

### Changed files

| File | Type |
|------|------|
| `.gitattributes` | Config |
| `deploy/systemd/alpaca-forward-truth-contract-run.sh` | Shell wrapper |
| `deploy/systemd/alpaca-forward-truth-contract.service` | Systemd unit |
| `deploy/systemd/alpaca-forward-truth-contract.timer` | Systemd timer |
| `reports/audit/ALPACA_FORWARD_TRUTH_CONTRACT_CSA_20260327_FORWARD_TRUTH_FINAL.md` | Documentation |
| `reports/audit/ALPACA_FORWARD_TRUTH_CONTRACT_IMPLEMENTATION_20260327_FORWARD_TRUTH_FINAL.md` | Documentation |
| `reports/audit/ALPACA_FORWARD_TRUTH_CONTRACT_SCHEDULER_20260327_FORWARD_TRUTH_FINAL.md` | Documentation |
| `scripts/audit/alpaca_forward_truth_contract_runner.py` | Python runner |
| `scripts/audit/alpaca_strict_six_trade_additive_repair.py` | Python repair |
| `scripts/audit/run_alpaca_forward_truth_contract_deploy.py` | Python deploy helper |

---

### Findings

| Severity | Location | Description |
|----------|----------|-------------|
| **LOW** | `scripts/audit/alpaca_forward_truth_contract_runner.py:26-30` | Uses `importlib.util.spec_from_file_location` + `spec.loader.exec_module` to dynamically load the repair module. The path is hard-coded to a known repo file (`alpaca_strict_six_trade_additive_repair.py`), so this is safe, but dynamic module loading is worth noting for audit trail purposes. |
| **LOW** | `scripts/audit/alpaca_forward_truth_contract_runner.py:104` | `subprocess.run(cmd, ...)` invoked with an explicit argument list (not `shell=True`) and a 600-second timeout. This is the correct safe pattern. No issue. |
| **LOW** | `deploy/systemd/alpaca-forward-truth-contract.service:8` | Service runs as `WorkingDirectory=/root/stock-bot` under the root user (implicit from systemd). Consider adding `User=` directive for least-privilege if feasible. |
| **MEDIUM** | _Pre-existing, not in this push:_ `reports/_daily_review_tools/run_droplet_*.py` (5 files) | Pattern: `exec(base64.b64decode(...))` to run Python payloads on a remote droplet via SSH. The payloads are generated locally from trusted code, not user input, so risk is bounded. However, the `exec(base64.b64decode(...))` pattern is inherently fragile and could become a vector if the payload source is ever compromised. |
| **MEDIUM** | _Pre-existing, not in this push:_ `scripts/run_uw_intel_on_droplet.py:141-176` | Multiple `os.system(f"...")` calls. Arguments are constructed from `os.sys.executable` and hard-coded script paths, but `os.system` should be replaced with `subprocess.run(...)` for better control and safety. |
| **MEDIUM** | _Pre-existing, not in this push:_ `force_cycle_run.py:15` | `exec(f.read(), {'__file__': activate_this})` to activate a virtualenv. Legacy pattern; consider replacing with `subprocess` or direct venv activation. |
| **MEDIUM** | _Pre-existing, not in this push:_ Multiple files (e.g., `execute_droplet_audit.py`, `deep_trading_diagnosis.py`, `check_droplet_trading_now.py`) | `subprocess.run(cmd, shell=True, ...)` with string commands. Commands appear to be hard-coded (not user-input-derived), so injection risk is low, but `shell=True` is best avoided. |

---

### Secrets & Credentials

**No secrets or credentials found in this push or in the repository.**

- All API keys (`ALPACA_KEY`, `ALPACA_SECRET`, etc.) are loaded from environment variables via `os.getenv()` or `Config` class attributes populated from `.env`. No hardcoded values.
- `.env` is properly gitignored (`.gitignore` contains `.env` and `*.env`).
- `.env.example` contains only placeholder names, no values.
- No `.pem` files, private keys, or bearer tokens found in the repository.
- `deploy_supervisor.py:383-384` sets `ALPACA_KEY = "INVALID_KEY"` for chaos testing only — not a real credential.

### Insecure Network Calls

**No insecure network patterns found in this push.**

- No `verify=False`, `CERT_NONE`, or `ssl._create_unverified_context` found anywhere in the repo.
- HTTP URLs found in the repo are exclusively `http://127.0.0.1:...` (localhost) for dashboard health checks — acceptable for internal loopback.
- External-facing Alpaca API calls use `https://` via the `alpaca-trade-api` SDK.

### Dependency Vulnerabilities

- `requirements.txt` exists. No pip-audit or automated vulnerability scanning detected in CI configuration for this push.
- **Recommendation:** Add `pip-audit` to CI pipeline if not already present.

---

### Recommendations

1. **No action required for this push.** All changed files are clean: no secrets, no unsafe patterns, no insecure network calls.

2. **Pre-existing (MEDIUM) — Consider addressing in a future cleanup:**
   - Replace `exec(base64.b64decode(...))` droplet execution pattern with file-based script execution (upload script, run script) to eliminate the `exec()` surface.
   - Replace `os.system()` calls in `scripts/run_uw_intel_on_droplet.py` with `subprocess.run()`.
   - Replace `shell=True` in diagnostic/investigation scripts with explicit argument lists.

3. **Best practice (LOW):**
   - Add a `User=` directive to the systemd service unit for least-privilege execution if the service does not require root.
   - Add `pip-audit` to CI for automated dependency vulnerability scanning.

---

### Verdict

**PASS** — No HIGH or CRITICAL findings. No GitHub issue required.
