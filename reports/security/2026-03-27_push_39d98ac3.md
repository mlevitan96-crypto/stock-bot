## Security Review (push to main)

**Commit / ref:** `39d98ac38b72ba33a7194fb51934222de2f1b362` — "Add Telegram failure paging and remediation notices"  
**Pushed by:** mlevitan96-crypto  
**Date:** 2026-03-27  
**Changed files (10):**
- `.github/workflows/python-tests.yml`
- `deploy/systemd/telegram-failure-detector.service`
- `deploy/systemd/telegram-failure-detector.timer`
- `reports/daily/2026-03-27/evidence/TELEGRAM_EXPECTED_WINDOWS_20260327_230000Z.md`
- `reports/daily/2026-03-27/evidence/TELEGRAM_FAILURE_ALERT_PROOF_20260327_230000Z.md`
- `reports/daily/2026-03-27/evidence/TELEGRAM_FAILURE_PAGING_CSA_VERDICT_20260327_230000Z.md`
- `reports/daily/2026-03-27/evidence/TELEGRAM_FAILURE_PAGING_SRE_VERDICT_20260327_230000Z.md`
- `scripts/governance/install_telegram_failure_detector_on_droplet.py`
- `scripts/governance/telegram_failure_detector.py`
- `tests/test_telegram_failure_detector.py`

---

### Findings

#### Changed Files (this push)

| Severity | Location | Description |
|----------|----------|-------------|
| **CLEAN** | `scripts/governance/telegram_failure_detector.py` | No hardcoded secrets. Credentials loaded from env vars at runtime via `scripts.alpaca_telegram`. `subprocess` calls use explicit argument lists (no `shell=True`). Timeouts set on all subprocess calls (60s, 120s, 300s). |
| **CLEAN** | `scripts/governance/install_telegram_failure_detector_on_droplet.py` | Uses `DropletClient` for SSH; no hardcoded credentials. Minor: duplicate `import sys` (line 6–7, cosmetic). |
| **CLEAN** | `deploy/systemd/telegram-failure-detector.service` | Reads `.env` via `EnvironmentFile=-`; no secrets in unit file. Runs as root (typical for systemd service on single-purpose droplet). |
| **CLEAN** | `tests/test_telegram_failure_detector.py` | No secrets. Uses monkeypatching for Telegram send. |
| **CLEAN** | `.github/workflows/python-tests.yml` | Standard CI config. No secrets exposed. |
| **CLEAN** | Evidence markdown files (4 files) | Documentation only; no secrets or tokens. |

#### Full Repository Scan (pre-existing, not introduced in this push)

| Severity | Location | Description |
|----------|----------|-------------|
| **MEDIUM** | `reports/_daily_review_tools/run_droplet_*.py`, `scripts/governance/_inspect_droplet_exit.py`, `scripts/governance/_fetch_three_run_metrics.py`, `scripts/governance/_run_compare_on_droplet.py` | **`exec(base64.b64decode(...))`** pattern used to ship Python payloads to the droplet over SSH. The base64 content is derived from local repo files (not user input), so this is **not RCE from external input**, but the pattern is fragile — any corruption of the payload file could lead to arbitrary code execution on the droplet. |
| **MEDIUM** | `execute_droplet_audit.py`, `deep_trading_diagnosis.py`, `comprehensive_no_positions_investigation.py`, `scripts/replay_week_multi_scenario.py`, `scripts/alpaca_telemetry_forward_proof.py`, `COMPREHENSIVE_DIAGNOSTIC_AND_FIX.py`, `COMPLETE_STRUCTURAL_INTELLIGENCE_DEPLOYMENT.py`, multiple `archive/` scripts | **`subprocess.run(..., shell=True, ...)`** used in diagnostic/deploy scripts. Command strings are constructed from controlled local variables (not user input), but `shell=True` with string concatenation increases attack surface if paths or arguments ever include untrusted data. |
| **MEDIUM** | `scripts/run_uw_intel_on_droplet.py` (lines 141, 159–176) | **`os.system()`** calls with f-string command construction. Arguments are locally controlled but `os.system` provides no output capture or error handling compared to `subprocess.run`. |
| **MEDIUM** | `complete_droplet_verification.py:85` | **`exec(import_stmt)`** where `import_stmt` comes from a hardcoded dict. Low practical risk but fragile pattern. |
| **MEDIUM** | `force_cycle_run.py:15` | **`exec(f.read(), ...)`** to activate a virtualenv. Standard venv activation pattern, low risk assuming droplet filesystem integrity. |
| **LOW** | `dashboard.py:6811`, `main.py:14582` | Flask `app.run(host="0.0.0.0")` binds to all interfaces. The dashboard has HTTP Basic Auth (constant-time comparison via `secrets.compare_digest`). Ensure firewall rules restrict port 5000 access on the droplet. |
| **LOW** | `deploy_dashboard_ssh_direct.py`, `deploy_dashboard_via_ssh.py`, `deploy_dashboard_fixes_ssh.py` | Print `http://104.236.102.57:5000` URLs (plain HTTP to droplet public IP). Dashboard traffic is unencrypted. Consider adding a TLS reverse proxy (nginx/caddy) or restricting to SSH tunnel access. |
| **LOW** | `scripts/dashboard_verify_all_tabs.py` | Default `DASHBOARD_BASE_URL` is `http://127.0.0.1:5000` (localhost, acceptable for on-droplet use). |
| **LOW** | `dashboard.py:5090`, `sre_monitoring.py:598`, `comprehensive_system_check.py:56` | HTTP calls to `localhost:8081` for health checks. Acceptable for local inter-process communication. |
| **LOW** | `requirements.txt` | No `pip-audit` in CI. Consider adding `pip-audit` step to `.github/workflows/python-tests.yml` to catch dependency CVEs. Pinned versions look current but `urllib3==1.26.20` is in the 1.x line (forced by `alpaca-trade-api` compatibility). |

---

### Recommendations

1. **No action required for this push.** The 10 changed files are clean — no secrets, no unsafe patterns, no insecure network calls.

2. **Pre-existing (MEDIUM) — Harden remote code execution patterns:**
   - The `exec(base64.b64decode(...))` droplet-payload pattern should be replaced with `scp` + `python <script>` to avoid code-in-string execution. Lower priority since the payloads are locally sourced.
   - Migrate `os.system()` calls in `scripts/run_uw_intel_on_droplet.py` to `subprocess.run()` with argument lists.
   - Audit all `shell=True` subprocess calls; convert to argument-list form where feasible.

3. **Pre-existing (LOW) — Network hardening:**
   - Add TLS termination (nginx/caddy) in front of the dashboard on the droplet, or restrict access via SSH tunnel / firewall.
   - Add `pip-audit` to the CI pipeline for automated dependency vulnerability scanning.

4. **No HIGH or CRITICAL findings.** No GitHub issue required.

---

*Automated security review by Cursor Cloud Agent.*
