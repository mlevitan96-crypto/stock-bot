# Sign-off v5 (2026-02-18)

## Multi-model oversight

### Execution plan review (before run)

- **Adversarial:** Restart must be no-overlay; proof must show no GOVERNED_TUNING_CONFIG. Poll may time out if no new exits; diagnostics then required.
- **Quant:** exit_quality_metrics requires new exits after restart; join fix (trade_id) makes blame classifiable when entry_score is present on joined rows.
- **Product:** PASS only if exit_quality > 0 and unclassified_pct < 100% (or bounded with explicit reason). One lever authorized on PASS.


### Results review (after run)

- **Adversarial:** exit_quality_metrics count=0; timed_out=True. Blame unclassified_pct=100.0. Pass criteria: eq>0 and (uncl<100 or explicit reason).
- **Quant:** joined_count=2000, weak_entry_pct=0.0, exit_timing_pct=0.0.
- **Product:** FAIL: exit_quality_metrics not observed on new exits (with_exit_quality_metrics == 0); Poll timed out (30 min); no new exits with exit_quality_metrics in sample.; Blame not classifiable: unclassified_pct >= 100% (or provide explicit field-level reason in verification).


## Criteria
- [x] exit_quality_metrics on new exits: **0** (non-zero required for PASS)
- [x] Blame classifiable: unclassified_pct = 100.0% (PASS if < 100% or explicit reason)

## Result: **FAIL**

## Blockers
- exit_quality_metrics not observed on new exits (with_exit_quality_metrics == 0)
- Poll timed out (30 min); no new exits with exit_quality_metrics in sample.
- Blame not classifiable: unclassified_pct >= 100% (or provide explicit field-level reason in verification).

STOP. No tuning until PASS.