# Git Token Setup

To configure GitHub token on droplet, run this command (replace YOUR_TOKEN):

```bash
cd ~/stock-bot
git remote set-url origin "https://YOUR_TOKEN@github.com/mlevitan96-crypto/stock-bot.git"
```

This configures git to use the token automatically for all operations.

